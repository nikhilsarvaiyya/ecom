import { UniqueWordFromArrayPipe } from './unique-word-from-array.pipe';

describe('UniqueWordFromArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new UniqueWordFromArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
