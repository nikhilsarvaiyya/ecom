import { Role } from './role';

export class Product {
    id?: string;
    name?: string;
    image?: object;
    price?: string;
    category?: string;
    description?: string;
    variants?: object;
}